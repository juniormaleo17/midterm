// setup your IIFE (Immediately Invoked Function Expression)
(function () {
"use strict";

(function (app) {
    "use strict"

    // About Button Click event handler

    function injecte() {
        // local variable
        let paragraphs = paragraphs.txt;

        
        console.log("Title: " + paragraphs);

    }


    //window.onload = Start;

    window.addEventListener("load", inject);


    // these are all app properties
    app.paragraphs = paragraphs.txt;


})(app || (app = {}));

// YOUR CODE GOES HERE


})();

